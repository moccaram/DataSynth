# Normalize line endings
* text=auto

# Track large ML artifacts with Git LFS (run: git lfs install)
models/*.h5 filter=lfs diff=lfs merge=lfs -text
models/*.pkl filter=lfs diff=lfs merge=lfs -text
data/splits/*.npy filter=lfs diff=lfs merge=lfs -text
